# CLAUDE.md

給所有在這個 repo 工作的 AI 看的規則。違反就停下、回報、等使用者決定。

---

## 第一層：紅線（違反就停下、不要動手）

1. **不虛構**
   - `01-data-brain/` 裡沒有的數字、案例、口頭禪 — 不准掰
   - 不確定就說「資料裡沒有、要不要補」、不要編

2. **改之前先講**
   - 不可逆動作（刪檔、改權限、`git push --force`、改 `.env`、撤銷已發 commit）一律先問
   - 只改使用者要求的部分、不要順手改旁邊的 code 或 comment
   - 改 X 會連動 Y、先列出來問、不要默默改

3. **缺核心節 → 不准生短影音腳本**
   - `01-data-brain/brand.md` 缺以下任一節、AI 拒絕生成腳本：
     - [3] 內容調性 + 標誌句
     - [4] 角度矩陣
     - [5] 素材日誌（至少 5 筆）
   - 提示使用者先補、不要硬寫

4. **規則寫在資料層、不寫進 prompt**
   - 能用 schema、lint、檔案結構擋的 — 不要靠 AI 自律
   - 修 bug 之前先想：能不能用規則 / 設定檔 / 資料結構擋掉？最後才寫新 code

---

## 第二層：資料地圖（誰是 SSoT、什麼時候讀什麼）

| 檔案 | 角色 | 何時讀 |
|---|---|---|
| `01-data-brain/index.md` | 資料地圖 SSoT | 開始任何任務前先讀 |
| `01-data-brain/brand.md` | 品牌靈魂、慢變、人填 | 寫腳本、改文案、做選題前 |
| `01-data-brain/brand-summary.md` | 速查版（衍生、不可手改） | SessionStart 自動注入 |
| `01-data-brain/cases.md` | 爆款案例庫 | 寫腳本要舉例、找參考結構時 |

**不要每次都翻全部** — 看任務從 `index.md` 決定讀哪幾個。

**衍生檔不可手改**：`brand-summary.md` 是從 `brand.md` 提煉的、要改改原檔。

---

## 第三層：日常習慣

1. **切換不同類型任務時、開新對話**（context 不要混）
2. **方向錯了用 rewind 倒回去**、不要在錯的方向上繼續加東西
3. **警告類機制必須走完四階段**：警告 → 自動修補 → 通知使用者 → 擋住新任務。只印警告不修、3 個月後變噪音
4. **犯錯先記成軟規則**、觀察反覆出現再升級成這份檔的硬規則、不要一次寫死
